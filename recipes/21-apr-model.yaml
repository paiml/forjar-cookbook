# Recipe: 21-apr-model
# Tier: 1+2+3 (validate, container with tiny model, GPU bare-metal)
# Idempotency: strong
# Budget: first_apply < 120s, idempotent < 5s
#
# Compiled APR model binary pipeline: pull model from HuggingFace,
# convert to APR format, compile into self-contained binary, deploy
# as systemd service with health monitoring.

version: '1.0'
name: apr-model-pipeline
description: "APR compiled model pipeline with download, conversion, compilation, and service deployment"

includes:
  - includes/policy-defaults.yaml
  - includes/notify-hooks.yaml

params:
  model_name: smollm2
  install_dir: /opt/apr
  serve_port: "8080"
  gpu_backend: cpu
  user: root

machines:
  model-host:
    hostname: model-host
    addr: container
    user: root
    arch: x86_64
    transport: container
    container:
      runtime: docker
      image: forjar-test-target
      name: forjar-cookbook-21-apr
      ephemeral: false
      privileged: false
      init: true

resources:
  apr-dirs:
    # Deploy managed configuration file
    type: file
    machine: model-host
    state: directory
    path: "{{params.install_dir}}/models"
    owner: "{{params.user}}"
    mode: '0755'
    tags: [apr, dirs]
    resource_group: apr-model

  apr-bin-dir:
    # Deploy managed configuration file
    type: file
    machine: model-host
    state: directory
    path: "{{params.install_dir}}/bin"
    owner: "{{params.user}}"
    mode: '0755'
    tags: [apr, dirs]
    resource_group: apr-model

  model-download-script:
    # Deploy managed configuration file
    type: file
    machine: model-host
    path: /usr/local/bin/apr-model-download.sh
    owner: "{{params.user}}"
    mode: '0755'
    content: |
      #!/bin/bash
      set -euo pipefail
      MODEL_DIR="{{params.install_dir}}/models"
      echo "Model download would fetch from HuggingFace..."
      echo "For CI: using placeholder model"
      echo '{"model": "{{params.model_name}}", "format": "gguf", "quantization": "q4_k_m"}' > \
        "$MODEL_DIR/model-info.json"
    # Dependency ordering
    depends_on: [apr-dirs]
    tags: [apr, download]
    resource_group: apr-model

  model-convert-script:
    # Deploy managed configuration file
    type: file
    machine: model-host
    path: /usr/local/bin/apr-model-convert.sh
    owner: "{{params.user}}"
    mode: '0755'
    content: |
      #!/bin/bash
      set -euo pipefail
      echo "Model conversion: GGUF -> APR format"
      echo "apr convert --input source.gguf --output model.apr --quantization q4_k_m"
      touch {{params.install_dir}}/models/model.apr
    # Dependency ordering
    depends_on: [model-download-script]
    tags: [apr, convert]
    resource_group: apr-model

  model-compile-script:
    # Deploy managed configuration file
    type: file
    machine: model-host
    path: /usr/local/bin/apr-model-compile.sh
    owner: "{{params.user}}"
    mode: '0755'
    content: |
      #!/bin/bash
      set -euo pipefail
      echo "Model compilation: APR -> self-contained binary"
      echo "apr compile --model model.apr --output model-server --backend {{params.gpu_backend}}"
      cat > {{params.install_dir}}/bin/model-server <<'STUB'
      #!/bin/bash
      echo "APR model server (placeholder) listening on port ${1:-{{params.serve_port}}}"
      STUB
      chmod 755 {{params.install_dir}}/bin/model-server
    # Dependency ordering
    depends_on: [model-convert-script, apr-bin-dir]
    tags: [apr, compile]
    resource_group: apr-model

  service-unit:
    # Deploy managed configuration file
    type: file
    machine: model-host
    path: /etc/systemd/system/apr-model.service
    owner: "{{params.user}}"
    mode: '0644'
    content: |
      [Unit]
      Description=APR Compiled Model Server
      After=network.target

      [Service]
      Type=simple
      User={{params.user}}
      ExecStart={{params.install_dir}}/bin/model-server --port {{params.serve_port}}
      Restart=on-failure
      RestartSec=10
      Environment=APR_GPU_BACKEND={{params.gpu_backend}}

      [Install]
      WantedBy=multi-user.target
    depends_on: [model-compile-script]
    tags: [apr, service]
    resource_group: apr-model

  health-check-script:
    type: file
    machine: model-host
    path: /usr/local/bin/apr-health-check.sh
    owner: "{{params.user}}"
    mode: '0755'
    content: |
      #!/bin/bash
      curl -sf http://localhost:{{params.serve_port}}/health 2>/dev/null && \
        echo "OK: model server healthy" || \
        echo "WARN: model server not responding"
    depends_on: [service-unit]
    tags: [apr, health]
    resource_group: apr-model

  health-cron:
    type: cron
    machine: model-host
    schedule: "0 */6 * * *"
    command: "/usr/local/bin/apr-health-check.sh 2>&1 | logger -t apr-model"
    name: apr-model-health
    depends_on: [health-check-script]
    tags: [apr, monitoring]
    resource_group: apr-model

  build-manifest:
    type: file
    machine: model-host
    path: "{{params.install_dir}}/manifest.json"
    owner: "{{params.user}}"
    mode: '0644'
    content: |
      {
        "model_name": "{{params.model_name}}",
        "model_format": "gguf",
        "quantization": "q4_k_m",
        "compile_target": "binary",
        "gpu_backend": "{{params.gpu_backend}}",
        "binary_path": "{{params.install_dir}}/bin/model-server",
        "serve_port": {{params.serve_port}}
      }
    depends_on: [model-compile-script]
    tags: [apr, manifest]
    resource_group: apr-model

policy:
  failure: continue_independent
  parallel_machines: false
  tripwire: true
  lock_file: true
  ssh_retries: 3
  pre_apply: "echo 'Starting APR model pipeline'"
  post_apply: "echo 'APR model pipeline complete'"
  notify:
    on_success: "echo 'Model compiled and deployed'"
    on_failure: "echo 'Model pipeline failed — check stages'"
    on_drift: "echo 'Model binary drift detected'"

outputs:
  binary_path:
    value: "{{params.install_dir}}/bin/model-server"
    description: "Compiled model server binary"
  serve_port:
    value: "{{params.serve_port}}"
    description: "Model inference HTTP port"
