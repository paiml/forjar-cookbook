# OpenTofu-Inspired Patterns

Patterns borrowed from OpenTofu/Terraform, adapted to forjar's sovereign model.

Recipes #30-39 cover saved plan files (TOCTOU safety), JSON plan format,
check blocks (post-apply health assertions), lifecycle protection rules,
moved blocks, refresh-only mode, resource targeting, a native testing DSL,
state encryption, and cross-config output references.
